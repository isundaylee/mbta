!!!
%html
  %head
    %title My MBTA Schedule
    %link{rel: "stylesheet", href: "styles.css"}/
    %meta{name: 'viewport', content: 'width=device-width, initial-scale=1'}/
    %link{rel: 'apple-touch-icon', sizes: '120x120', href: 'touch-icon-120.png'}
    %link{rel: 'apple-touch-icon', sizes: '152x152', href: 'touch-icon-152.png'}
    %link{rel: 'apple-touch-icon', sizes: '167x167', href: 'touch-icon-167.png'}
    %link{rel: 'apple-touch-icon', sizes: '180x180', href: 'touch-icon-180.png'}
  %body
    %h1 My Transit Dashboard
    - bluebikes_status.each do |station, data|
      %h2= station
      %p.arrivals
        = "#{data[:bikes]} bikes, #{data[:docks]} docks"
    - mbta_schedule.each do |stop, arrivals|
      %h2= stop
      %p.arrivals
        - if arrivals.empty?
          No predictions.
        - else
          = arrivals.take(5).map { |a| "#{(a / 60).to_i} mins" }.join(", ")
