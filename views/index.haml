!!!
%html
  %head
    %title My MBTA Schedule
    %link{rel: "stylesheet", href: "styles.css"}/
    %meta{name: 'viewport', content: 'width=device-width, initial-scale=1'}/
  %body
    %h1 My MBTA Schedule
    - schedule.each do |stop, arrivals|
      %h2= stop
      %p.arrivals
        - if arrivals.empty?
          No predictions.
        - else
          = arrivals.take(5).map { |a| "#{(a / 60).to_i} mins" }.join(", ")
